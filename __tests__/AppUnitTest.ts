// import React from 'react';
// import { mount, configure } from 'enzyme';
// import Adapter from '@wojtekmaj/enzyme-adapter-react-17';
// import App from '../client/components/App';

// configure({ adapter: new Adapter() });

// describe('App Unit Tests', () => {
//   let wrapper;
//   it('should render Login Page first', () => {
//     wrapper = mount(App());
//     expect(wrapper.find('#username')).not.toEqual({});
//     expect(wrapper.find('#password')).not.toEqual({});
//     expect(wrapper.find('#signUpBtn')).not.toEqual({});
//     expect(wrapper.find('#loginBtn')).not.toEqual({});
//   });
// });
describe('dummy test', () => {
  it('is a placeholder', () => {
    expect(1).toBe(1);
  });
});
